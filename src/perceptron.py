import numpy as np
from scipy.spatial import distance


def perceptron_train(X, Y):
    update_flag = 0
    print("\nperceptron", X, Y, sep='\n', end='\n\n')
    while update_flag:
        update_flag = 0
    temp = (0, 0)
    return temp

def perceptron_test(X_test, Y_test, w, b):
    pass
